# Examples

Example implementations using Farcaster Kit.

## Script Example

Basic command-line script demonstrating HTTP and contract features.

```bash
cd script
bun install
bun run start
```

Tests:
- HTTP methods (hub info, casts, user data, FID lookup)
- Contract reads (FID, storage, custody, recovery, pricing)

